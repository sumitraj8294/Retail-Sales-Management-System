name: Ping Render Backend

on:
  schedule:
    - cron: "*/5 * * * *"   
  workflow_dispatch:       

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Backend
        run: |
          curl -fsS https://retail-sales-management-system-xnp0.onrender.com/health || echo "Backend not responding"
